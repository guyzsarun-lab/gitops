name: devops-toolchain

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - applicationset/devops-toolchain/**

jobs:
  docker-build:
    uses: ./.github/workflows/reusable-docker-build.yaml
    with:
      path: applicationset/devops-toolchain
name: devops-toolchain

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - applicationset/devops_toolchain/**

jobs:
  docker-build:
    uses: ./.github/workflows/reusable-docker-build.yaml
    with:
      path: applicationset/devops_toolchain
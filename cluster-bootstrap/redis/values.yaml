redis:
  fullnameOverride: "redis"
  architecture: "standalone"
  auth:
    enabled: false
  replica:
    persistence:
      enabled: false
  image:
    registry: docker.io
    repository: library/redis
    tag: 8.2.3-alpine